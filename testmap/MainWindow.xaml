<Window x:Class="testmap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dxm="http://schemas.devexpress.com/winfx/2008/xaml/map"
        xmlns:local="clr-namespace:testmap"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Ellipse Width="300" Height="300" Fill="LightGray" Stroke="Black" StrokeThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Ellipse.Effect>
                <DropShadowEffect ShadowDepth="5" BlurRadius="10"/>
            </Ellipse.Effect>
        </Ellipse>
        <dxm:MapControl Name="dg1" Width="250" Height="250" HorizontalAlignment="Center" VerticalAlignment="Center">
            <dxm:ImageLayer x:Name="dg2">
                <dxm:BingMapDataProvider/>
            </dxm:ImageLayer>
            <!-- اینجا می‌توانید تنظیمات نقشه خود را اضافه کنید -->
        </dxm:MapControl>


    </Grid>
</Window>
